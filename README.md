# Kobciye — School Management System

- **landing/** — Landing page (fur `landing/index.html` browser-ka)
- **mobile/** — Expo React Native app (`cd mobile && npm install && npx expo start`)
- **supabase/** — Phase 2 backend: database schema, migrations, RLS, storage (eeg `supabase/README.md`)
