/* ============================================================
   Kobciye — public landing configuration (standalone landing/index.html)

   These are PUBLIC business values, not secrets. Set your real ones here.

   LANDING_WHATSAPP_NUMBER — the Kobciye owner's business WhatsApp number for
   the school-enquiry form. DIGITS ONLY: country code + number, with NO '+',
   spaces or dashes (e.g. Somaliland 252 + 63 7373367 => "252637373367").

   APP_LOGIN_URL — the deployed Kobciye app login URL that the landing "Login"
   button opens (the real email/password app login). Leave as "#" until the app
   is deployed.
   ============================================================ */
window.KOBCIYE_LANDING_WHATSAPP_NUMBER = '252637373367';
window.KOBCIYE_APP_LOGIN_URL = '#';
